import styled from 'styled-components';

export const ToastsStyled = styled.div`
  position: fixed;
  padding: 10px;
  width: 100%;
  max-width: 450px;
  max-height: 100vh;
  overflow: hidden auto;
  right: 0;
  bottom: 0;
  z-index: 1500;
`;
